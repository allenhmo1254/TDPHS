using UnityEngine;
using System.Collections;

public class PlayerTranscriptAnimation : MonoBehaviour {

//	private Animator anim;
	// Use this for initialization
	void Start () {
//		anim = this.GetComponent<Animator> ();
	}
	
	// Update is called once per frame
	void Update () {
//		print ("Mathf.Abs(rigidbody.velocity.x) = " + Mathf.Abs (rigidbody.velocity.x));
//		print ("Mathf.Abs(rigidbody.velocity.z) = " + Mathf.Abs (rigidbody.velocity.z));
//		if (Mathf.Abs(rigidbody.velocity.x) > 0 || Mathf.Abs(rigidbody.velocity.z) > 0) {
//			anim.SetBool("Move", true);
//		} else {
//			anim.SetBool("Move", false);
//		}
	}
}
